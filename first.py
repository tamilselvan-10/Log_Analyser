import logging
import numpy as np
import matflotlib.yplot as mtp

x= numpy.data(x)
''' basic for data are stored in the git hub for quick access and manage the function tto maintain the labeled function'''
import math
